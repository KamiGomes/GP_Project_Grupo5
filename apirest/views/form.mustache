<html>
    <head>
      <meta http-equiv="Content-type" content="text/html; charset=utf-8">
      <title>{{title}}</title>
    </head>
    <header>
    </header>
    <body>
        <h1>{{formTitle}} {{formAction}}</h1>
    <form method="{{formMethod}}">
      {{#properties}}
      <div>
          <label >{{name}}:</label>
          {{^isDropdown}}
          <input type={{type}} value="{{value}}" name="{{nameLower}}" {{#required}}required{{/required}}/>
          {{/isDropdown}}
      </div>
      {{/properties}}
        {{^edit}}
        <input type="submit" value="Submit">
        {{/edit}}
    </form>
        {{#edit}}
        <input type="submit" value="Submit" onClick="putRquest()">
        {{/edit}}
    </body>
</html>
